require "bundler/gem_tasks"
require "rspec/core/rake_task"

RSpec::Core::RakeTask.new(:spec)

task :default => :spec

desc "Build bibliografia-0.1.0.gem into the pkg directory"
task :build do
	sh ""
end

desc "Build and install bibliografia-0.0.1.gem into system gems"
task :install do
	sh ""
end

desc "Create tag v0.0.1 and build and push  bibliografia-0.0.1.gem to Rubygems"
task :release do
	sh ""
end

desc "Run RSpec code bibliografia"
task :spec do
	sh ""
end





